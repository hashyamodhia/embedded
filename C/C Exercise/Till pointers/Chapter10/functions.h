#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void avg (int numstu, int marks[numstu], float *average);
int cnt (char name []);
void triple (int arr1[], int arr2[]);
void change (int arr1[], int arr2[]);
