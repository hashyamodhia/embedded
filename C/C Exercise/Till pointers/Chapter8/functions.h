#include <stdio.h>
#include <stdlib.h>

int odd (int a[]);
int even (int a[]);
void sequence ();
int noof1 (int num);
int factorial (int num);
int cal (int a, int b);
int Addition (int a, int b);
int Subtraction (int a, int b);
float Division (int a, int b);
