#include <stdio.h>
#include <stdlib.h>

struct SI1
{
    float interest1;
    int bamount1;
    float rate1;
    float total1;
};

void display_amount1(int num7, struct SI1 *simple2);

struct SI2
{
    float interest2;
    int bamount2;
    float rate2;
    float total2;
};

void display_amount2(int num8, struct SI2 *simple4);
