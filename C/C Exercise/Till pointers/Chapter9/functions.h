#include <stdio.h>
#include <stdlib.h>

char *mon (int monno);
float percentage(int numstu, int numsub, int marks[numstu][numsub]);
int high(int number[]);
int ascending (int number[], int n);
int replaces (int number[], int n);
